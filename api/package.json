{
  "name": "pamn-mock-api",
  "version": "0.0.1",
  "scripts": {
    "api": "json-server api/db.json --routes api/routes.json --no-cors=true"
  },
  "private": true,
  "dependencies": {
    "json-server": "^0.16.2"
  }
}
